# Server Configuration
PORT=3000

# Base URL for the server (used for generating image URLs in /images endpoint)
# Example: https://lbatch-nuc12.local or http://localhost:3000
BASE_URL=http://localhost:3000

# PWA/HTTPS Configuration (for production with mkcert)
# Domain/hostname for HTTPS (required for PWA Share Target API)
# This should match the hostname you generated certificates for
# Example: lbatch-nuc12.local or your-server.local
DOMAIN=lbatch-nuc12.local

# Caddy HTTP/HTTPS Ports (defaults: 3001/3002)
HTTP_PORT=3001
HTTPS_PORT=3002
